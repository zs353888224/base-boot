description = 'env of project'

sourceSets {
    def p = profile
    if (project.hasProperty("env")) {
        p = "${env}"
    }
    main {
        resources {
            srcDir("src/main/conf/" + p)
        }
    }
}